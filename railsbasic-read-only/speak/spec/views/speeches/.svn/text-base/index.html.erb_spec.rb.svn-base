require 'spec_helper'

describe "speeches/index" do
  before(:each) do
    assign(:speeches, [
      stub_model(Speech),
      stub_model(Speech)
    ])
  end

  it "renders a list of speeches" do
    render
    # Run the generator again with the --webrat flag if you want to use webrat matchers
  end
end
